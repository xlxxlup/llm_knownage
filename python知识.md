深拷贝：用list()或[:]创建新列表，**避免引用传递**

self.p_path = list(self.path)

装饰器是 Python 非常强大的**语法糖**，核心作用：**在不修改原函数代码的前提下，给函数增加新功能**

工厂模式：将对象的创建对象和使用对象分开

builder模式:  分步骤创建复杂对象

单例：一个类只有一个对象



对比一下常见的拼接函数：

| 函数          | 作用                   |
| ------------- | ---------------------- |
| `torch.stack` | **新增一个维度**来拼接 |
| `torch.cat`   | 沿**已有维度**拼接     |



查看指定端口（例如 8080） 

ss -tulnp | grep 端口号

参数说明：

- `-t`：TCP 端口
- `-u`：UDP 端口
- `-l`：只看监听中的端口
- `-n`：不解析域名 / 服务名，纯数字
- `-p`：显示占用该端口的进程 PID / 程序名



`grep` 就是 Linux 里**用来在文本 / 命令输出里搜索关键词**的工具

### cat 文件名 直接把文件内容全部打印到屏幕

### `head 文件名`默认显示前 10 行