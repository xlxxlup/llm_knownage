# 实习项目话术（润色版）

## 项目一：垂直领域客服对话大模型

### 项目背景

这个公司的核心业务是为医疗（植发、皮肤科等）、美容等垂直领域提供智能客服机器人。访客通过广告入口点击进入咨询，机器人表面上为访客答疑，实际核心目标是引导留联（获取联系方式），实现从流量到线索的转化。所以模型需要在"专业答疑"和"高效套联"之间取得平衡。

### 我的工作——数据治理

我的第一个核心工作是围绕45万条(6万条拆分)真实多轮对话数据，搭建一套标准化的数据筛选与合成流水线，最终沉淀出10万条高质量SFT样本。整个流程分四步：

1. **轮次过滤**：根据业务特性，原始对话轮次在1-23轮，有效对话一般在15轮以内，业务目标是5-10轮完成目标，所以先按对话轮数截取1-15轮的数据，并过滤掉回复过长(80)或过短(5)的，过滤掉过长（通常是无效拉扯）和过短（无实质内容）的对话。(剩42万)

2. **语义初筛**：通过搜索词/意图关键词（问诊意图如疾病名称、症状名称；用药咨询等，共35种(疾病+意图)）做第一轮过滤，剔除与核心业务无关的对话。前三轮访客必须表达出自己的目的(疾病、症状)(剩35万)

3. **Embedding语义去重**：经过前两步过滤后数据量仍然很大，其中存在大量语义高度相似的对话（不同访客描述相同症状、相同问诊流程），需要去重来保证数据多样性。具体实现：(剩27万)
   - **Embedding编码**：用Qwen3-Embedding-8B(4096)模型，对所有对话文本批量编码为向量，得到每条对话的语义向量表示
   - **聚类分组**：用K-Means聚类对embedding向量进行聚类，将语义相近的对话归到同一个簇中。聚类数K可以根据业务经验设定（比如按疾病类型×意图的组合数作为参考），也可以用肘部法则确定
   - **簇内去重**：在每个聚类内部计算两两余弦相似度，相似度超过阈值的视为重复，每个重复组只保留一条代表样本（可以保留对话轮数最多或质量最高的一条）。这样做的好处是：先聚类缩小了比较范围，簇内数据量远小于全量，簇内两两比较的计算量可控
   - **阈值确定**：在随机抽样的5000条子集上，分别用0.80、0.85、0.90、0.92、0.95几个档位去重，每个档位随机抽100对"被判为重复"的数据人工判断是否真的语义重复，最终选择误杀率最低且去重率合理的阈值（中文对话场景下一般0.88-0.92之间比较合适）

4. **LLM-as-a-Judge深度质检**：最后用大模型结合业务规则（套联相关、对话质量、医学合规、回复风格与可持续性。如回复是否包含违规信息、是否完成问诊闭环等）对每条数据进行质量判定，过滤掉回复质量不达标的数据。(11万)

> 这个四步流程体现的是"量到质"的递进式治理思路，面试时可以说"不是一步到位，而是逐层提纯"。

### 我的工作——SFT训练与重复问诊问题修复

基座模型用的是Qwen3-8B。SFT之后基本功能满足要求，

原始数据集测试数据: 重复问诊率:12.7% 整体问题率:19.1%
微调完后的数据: 重复问诊率:10.6% 整体问题率:13%

loss = 1.2

/data2/xiangl/projtct/ms-swift-main/ms-swift-main/swift/cli/sft.py --model_type qwen3 --model /data/public/Qwen/Qwen3/Qwen3-8B/ --dataset /data2/xiangl/projtct/ms-swift-main/ms-swift-main/train_data/0225/summary_data.jsonl --num_train_epochs 1 --per_device_train_batch_size 16 --per_device_eval_batch_size 2 --gradient_accumulation_steps 16 --learning_rate 1e-4 --weight_decay 0.01 --warmup_ratio 0.05 --eval_strategy steps --eval_steps 100 --split_dataset_ratio 0.01 --optim adamw_torch --logging_steps 2 --save_steps 100 --save_total_limit 4 --lr_scheduler_type cosine --bf16 true --gradient_checkpointing true --lora_rank 32 --lora_alpha 64 --system 你是一名皮肤科负责在线医疗咨询的医生客服，任务是根据已有对话历史（访客和医生客服的交替发言，相互交替发言1次算1轮），遵循回复策略给出接下来医生客服的回复。核心目标是：客服通过问诊（询问症状/特征/时间）和答疑（解释病情/治疗方案）推进对话，从第3轮起开始套联（获取访客联系方式），最终促成访客留联。 --loss_scale last_round --lora_dropout 0.05 --max_length 4096 --load_from_cache_file false --output_dir /data2/xiangl/projtct/output/qwen3_8b_lora_pifu_dialogue --deepspeed zero2 --ddp_find_unused_parameters false --save_only_model true --report_to swanlab --swanlab_project qwen3-dialogue-model --swanlab_exp_name qwen3-dialogue-model_lora --swanlab_mode cloud --enable_thinking false

但暴露出一个核心问题——**重复问诊**。比如：
- 用户说"我孩子现在初中，长痘痘"，模型还会追问年龄
- 用户说"我老公背上长了很多水痘"，模型还会问"您是男性还是女性"

这个问题本质上是**过拟合**：未微调的基座模型通常不会重复提问，但微调后由于训练数据中"主动询问"的模式过强，覆盖了模型原有的上下文理解能力。

我先后尝试了三种方案：

- **方案一：数据增强**。具体实现如下：
  1. **分析问题根因**：先从SFT训练集中统计重复问诊的badcase，发现核心原因是数据分布不均——训练数据中用户描述信息的方式偏"直白"（如"男，25岁，脱发"）或者是以第一人称来问诊，模型学到的是"信息缺失就追问"。但真实场景中用户表达非常口语化和间接，这类样本在训练集中占比极低，模型没有学会"从间接表达中提取隐含信息"。
  2. **槽位维度梳理**：从badcase中归纳出三类最容易被模型漏识别的隐含信息槽位——**年龄**（学段"我家孩子上初二"、"快退休了"、"大学刚毕业"；年龄段"三十出头"、"快四十了"；代际关系"我婆婆"、"我儿子"）、**性别**（亲属称谓"我老公"→男、"我老婆"→女；职业暗示"我月经失调"→只可能为女生）、**症状/疾病**（口语化描述"头上长包"→可能是毛囊炎、"脸上有红点点"→可能是痤疮、"脚非常臭"-> 脚气；地方方言表述）。
  3. **合成流程**：
     - **选择模板数据**：从训练集中筛选满足以下条件的数据作为改写模板——① 对话中用户侧明确提到了年龄/性别/症状等关键信息（直白表达，如"男，25岁"），② 模型回复中正确识别了这些信息，没有重复追问。筛选方式是用大模型对训练集逐条打标，判断该条对话是否包含可隐含表达的关键信息，以及模型是否正确处理。最终筛选出一批符合条件的"正样本模板"
     - **按槽位分组**：将筛选出的模板按涉及的槽位类型分组（如"年龄+性别"组、"仅年龄"组、"症状"组等），确保每种槽位组合都有足够的模板覆盖
     - **批量改写**：对每条模板，用大模型进行改写。提示词的核心逻辑是：
       ```
       你是一个对话数据增强工具。以下是一段客服对话，其中用户侧直接/明确地表达了某些关键信息。
       请你对用户侧的发言进行改写，要求：
       1. 将用户明确提到的信息改为口语化、间接的表达方式
       2. 保持原始对话意图不变（不要改变用户想咨询的内容）
       3. 改写后的表达中，关键信息仍然可以被合理推断出来
       4. 只改写用户侧发言，客服侧回复保持不变

       【需要间接化的信息】：性别（原文"男"）
       【可选的间接表达方式】："我老婆说..."、"我女儿..."、"我是理工科的..."等

       【原始对话】：
       用户：男，25岁，最近脱发严重
       客服：您好，请问脱发大概多长时间了？

       【改写后对话】：
       用户：我今年25，最近头发掉得厉害，我老婆都说我发际线往后移了
       客服：您好，请问脱发大概多长时间了？
       ```
       每条模板生成3-5个不同的改写版本，增加表达多样性

     - **质量校验**：改写完成后用LLM做自动质检，检查两个维度——① 隐含信息是否仍可被合理推断（如改写后"我老婆说我发际线往后移了"是否还能推断出性别为男），② 对话逻辑是否通顺（改写后的用户发言是否自然、不突兀）。校验不通过的丢弃
  4. **训练集混合策略**：合成数据不单独训练，而是按5%-10%的比例混入原始SFT数据集中一起训练，避免合成数据过多导致分布偏移。
  5. **效果与局限**：数据增强后模型对口语化表达的理解确实有提升，但未能从根本上解决问题。原因在于：口语化表达的组合空间是开放的，合成数据很难穷举所有表达方式；短对话可以解决了，对话轮次长了之后还是有问题，本质问题不是"没见过这种表达"，而是模型在微调后丢失了"综合上下文判断是否需要追问"的能力。所以后续转向了方案二（自蒸馏）和方案三（信息槽位）。

- **方案二：自蒸馏（Self-Distillation，SDFT）**。

参考论文"Self-Distillation Enables Continual Learning"，具体实现如下：

learning_rate = 2e-5
num_generation = 8


  1. **核心思想**：学生模型和教师模型从同一个基座模型初始化（权重完全相同）。教师模型通过上下文学习（ICL）获取能力——将golden response放入教师模型的提示词中作为示例，教师模型基于这个带示例的提示rollout出多个回答；学生模型只接收原始提示（不含示例），目标是让自己的输出分布逼近教师模型的分布。本质上是用模型自身的ICL能力作为"软标签"来训练自己。

  2. **具体流程**：
     - **数据准备**：每条数据生成两种提示——学生提示（仅原始对话上下文）和教师提示（原始上下文 + golden response作为参考示例）
     - **生成阶段**：教师模型基于带示例的教师提示，通过vLLM生成多个候选回复（on-policy生成）
     - **损失计算**：将学生提示+生成的回复送入学生模型计算log概率，将教师提示+同一回复送入教师模型计算log概率，然后计算两者之间的KL散度作为损失
     - **参考模型同步**：每步训练后，用极小的混合系数（α=0.01）缓慢更新教师模型权重：`π_ref = α × π_θ + (1-α) × π_ref_prev`，保证教师模型不会偏离学生模型太远


  3. **损失函数**：核心是学生分布与教师分布之间的KL散度。代码中支持三种模式——α=0时为前向KL（让学生的分布覆盖教师的分布），α=1时为反向KL（让学生集中在教师的高概率区域），0<α<1时为Jensen-Shannon散度（两者的混合）。另外还有一个β参数控制对基座模型的KL正则化，防止偏离原始模型太远。同时还做了重要性采样校正，补偿vLLM生成和训练模型之间的分布差异。

   默认的是model-covering 的前向kl损失

  4. **缓解过拟合的原因**：
     - **On-policy训练**：训练信号来自模型自身当前状态的生成，而非静态的ground truth。模型被优化的目标是"模仿带示例时的自己"，而不是"记忆固定答案"，因此不会过拟合到特定模式上
     - **参考模型缓慢同步机制**：教师模型以极慢的速度（α=0.01）跟踪学生模型的更新，形成一个"移动锚点"，将模型的演化约束在一个安全的范围内，不会偏离太远
     - **KL正则化**：对基座模型的KL约束直接限制了参数空间的偏移幅度

  5. **相比直接SFT的优势**：
     - **SFT是off-policy的**：它强制模型去拟合固定的标注数据，即使这些数据的分布与模型当前的能力不匹配，模型也会被"硬拽"过去，容易导致原有能力丢失
     - **SDFT是on-policy的**：训练信号来自当前模型自己的生成，学习目标始终在模型"够得着"的范围内，是一种更温和的学习方式
     - **SFT直接复制模式**：模型倾向于记住训练数据中的特定表达模式，容易产生过拟合
     - **SDFT保留多样性**：教师模型通过ICL生成多样的回复，学生模型学习的是一个分布而非单一样本，保留了生成的多样性

  6. **效果与问题**：重复问诊确实减少了，说明自蒸馏对缓解过拟合是有效的。但出现了新问题——**啰嗦**。模型会生成类似"根据参考回答……怎么样怎么样，符合要求"这样的冗余文本。原因在于模型学到了教师提示中的参考示例模式，在回复时会把参考答案"念"出来而不是自然地融入对话。这个问题的根源在于教师提示中包含golden response作为上下文，模型在生成时会过度依赖这些上下文信息。所以最终转向了方案三（信息槽位体系）。

- **方案三（最终方案）：信息槽位体系**。这是最终落地的方案，具体实现如下：

  1. **槽位体系设计**：根据问诊业务梳理出核心信息槽位，每个槽位定义取值范围——**年龄**（具体数字或"未知"）、**性别**（男/女/"未知"）、**症状**（症状名称/"未知"）、**意图**（问诊/用药咨询/费用咨询/套联等）、**已问信息**（本次对话中已询问过的槽位列表）。

  2. **数据构造方式**：
     - **自动标注**：用一个能力较强的大模型（如DeepSeek/GPT）对对话历史进行槽位提取，输出格式化的槽位信息。提示词要求：根据当前对话上下文，提取已知的患者信息，未知的信息标记为"未知"。
     - **人工校验**：对自动标注的槽位进行抽样检查，确保提取的准确性。
     - **构造训练样本**：将原始对话的回复改写为 `槽位信息 + 原始回复` 的格式。例如：
       ```
       输入（对话历史）：用户：我老公脱发怎么治疗
       输出：[年龄：未知] [性别：老公 -> 男] [症状：脱发] [意图：问诊] [已问信息：无]
             您好，关于脱发的问题，请问您老公脱发大概多长时间了？是头顶稀疏还是发际线后移呢？
       ```
     - 关键是确保训练数据中：当槽位标记为"已知"时，回复中不重复询问该信息；当槽位标记为"未知"时，回复中应主动询问。这样模型就能学到"基于槽位状态决定是否追问"的逻辑。

  3. **训练策略**：将构造好的槽位数据混入原始SFT数据中（占比20%-30%），一起进行SFT训练。模型同时学习两个能力——① 从上下文中提取已知信息填充槽位 ② 基于槽位状态生成合理的回复。关键是**槽位和回复是一起训练的**，模型在生成回复之前必须先生成槽位，形成一个隐式的"先思考再回答"的过程。

  4. **推理阶段的使用**：线上推理时模型会自动在回复前生成槽位。处理方式是：直接输出槽位+回复的完整文本，前端展示时通过正则/后处理把 `[...]` 格式的槽位内容去掉，只展示回复部分给用户。如果用的是Qwen3这类支持thinking的模型，也可以把槽位放在 `<think...</think*>` 标签内，作为内部推理过程，对外只展示回复内容。

  5. **为什么这个方案有效**：
     - **显式状态追踪**：模型在每次回复前必须先判断"我现在知道什么、不知道什么"，这个判断过程被显式化、强制化了，不依赖模型隐式地"记住"
     - **容错性好**：即使模型对某个槽位的判断是错的（比如把"初中"判断为未知年龄），它也只是会去再确认一下，不会导致严重的对话错误；而SFT模型如果隐式记忆被破坏，可能连很明显的已知信息都会重复问
     - **可解释性强**：可以通过检查模型生成的槽位内容快速定位问题——如果槽位提取错误，说明需要增加对应类型的数据；如果槽位正确但回复仍重复问，说明回复生成部分需要优化
     - **与CoT的类比**：类似思维链让模型在数学推理中"写出计算过程"来提升准确率，槽位体系让模型在对话中"写出已知信息"来避免重复问诊

MedDialog : 医疗通用数据集

### 后续DPO工作

后续还做了DPO（Direct Preference Optimization），原因是不同客户对机器人回复风格有差异化需求——有的侧重"答疑+问诊"，有的侧重"答疑+福利套联"，用统一的SFT模型难以兼顾。通过DPO可以为不同客户场景定制偏好，不过这部分不是我的核心负责内容。

### 业务成果

最终将重复问诊率从10.6%降至5.9%，整体问题率从13%降至7.4%，显著提升了用户体验与留联转化效率。

---

## 项目二：垂直领域生成式奖励模型

### 项目背景与动机

这个项目是第二个阶段的核心工作。奖励模型在业务中有三个关键用途：
1. **强化学习的奖励信号来源**
2. **线上推理时从多个候选回复中选择最优回复**
3. **数据清洗与质量筛选**

之前用的是**判别式奖励模型**，但在实际业务中暴露了三个核心问题：

- **泛化性差**：公司服务很多客户，每家对回复风格都有细微差异，用一个判别式模型很难同时覆盖。
- **可解释性差**：线上模型生成3条回复，人工觉得1更好，但DPO打分却认为2更好，而且无法解释原因——判别式模型就是一个黑盒打分器。
- **可控性差**：不同客户的偏好不同，想用同一个模型适配所有客户基本行不通。

> 那为什么不用GPT、Kimi这类商业大模型直接做？其实完全够用，1、但业务数据涉及客户隐私，不能用闭源模型；2、开源的Kimi-2.5参数量太大，公司只有16张A800，显存不够。3、推理时间长。所以最终决定训一个32B级别的生成式奖励模型，用结构化推理来实现可解释、可控的偏好评判。

### 整体方案：SFT + DAPO

参考了多篇生成式奖励模型的论文，整体采用**SFT冷启动 + DAPO强化学习**的两阶段方案。

### 第一阶段：SFT冷启动

#### 数据准备（32K）

1. **基础数据（30K）**：复用之前训DPO的偏好数据，然后将候选对调换位置，数据量翻倍。

套联相关、对话质量、医学合规、回复风格与可持续性

2. **补充构造数据（2K）**：原始数据是模型rollout后人工标注的，回复主要存在业务层面的问题。但奖励模型应该评价的维度远不止业务，还涉及**违禁具体信息**（如给出具体用药剂量）、**自我说明**（如"我是一个AI助手"）等维度，这些在原数据集中几乎没有覆盖。所以我把现有对话数据 + 期望违反的规则一起作为提示词，让模型进行针对性改写。质量验证方式是：让大模型对改写后的数据多次打分，如果超过一半的评分能正确触发对应的新增规则，则视为构造成功。

> 这种"定向构造负样本"的思路很关键，面试时可以强调：**这不是随机增强，而是针对数据集的长尾缺陷做定向补全**。

#### 数据集划分

不是简单的随机划分，而是根据每条数据触发的**规则分布**分层划分成三部分。原因是：提示词中定义了28条评分规则（套联相关[1.1]-[1.6]、对话质量[2.1]-[2.15]、合规安全[3.1]-[3.4]、篇幅可继续性[4.1]-[4.4]），但不同规则在数据集中的命中频率差异很大——比如[2.1]（紧贴最后一轮）可能80%的数据都会命中，而[3.2]（违禁具体信息）可能只有2%的数据命中。如果随机划分，测试集中可能完全没有[3.2]的样本，就无法评估模型对该规则的判定能力。

具体做法：
1. **规则标注**：先对全部32K数据用大模型跑一遍评分，提取每条数据命中的规则编号列表（如某条数据输出了`<score>候选1:11.5[2.1,best],候选2:8[1.2,2.11]</score>`，则记录该数据涉及规则[2.1]、[1.2]、[2.11]）
2. **构建规则-数据映射**：统计每条规则被多少数据命中，识别出"低频规则"（如命中次数<200条的规则，这些是最容易在随机划分中丢失的）
3. **分层划分**：确保低频规则在每个子集中都有覆盖，具体步骤如下：
   - **按规则组合分桶**：将每条数据根据其命中的规则集合归入一个"桶"。例如命中规则`{[1.2], [2.1], [2.3]}`的所有数据归入同一个桶，命中`{[3.1], [4.2]}`的归入另一个桶
   - **处理低频桶**：对于数据量很少的桶（如某规则组合只有不到3条数据），强制将其分配到验证集，保证这些低频场景一定能被评估到
   - **按比例分配**：对于数据量充足的桶，按照约70%:15%:15%(22400:4800:4800)的比例分配到RL训练集、SFT训练集、验证集（比例可以微调，确保三个子集都够用）
   - **SFT训练集二次筛选**：分配到SFT子集中的数据还需要进一步筛选——只保留规则命中数量适中（3-6条）、且最终分数和规则与人工标注一致的样本，作为高质量冷启动数据
   - **校验覆盖率**：划分完成后统计每个子集中各条规则的命中次数，确保28条规则在每个子集中都至少有几十条以上的样本覆盖，低频规则至少有几条

最终划分为三个子集：
- **SFT冷启动训练集（约5K）**：用于SFT阶段的冷启动训练。从全量数据中筛选规则命中数量适中（3-6条）、且最终分数和规则与人工标注一致的较高质量样本。经过DeepSeek-R1蒸馏出思维链后作为SFT训练数据
- **RL强化学习训练集（约22K）**：用于DAPO强化学习阶段。使用剩余的大部分数据，RL阶段模型会自行rollout生成回复并计算奖励，不需要golden response中的思维链，但需要对话历史和候选回复作为输入
- **验证集（约5K）**：用于全流程的效果对比评估。之所以需要较大验证集，是因为28条规则中有不少低频规则（如[3.2]违禁具体信息可能只占2%），验证集太小会导致某些规则在验证集中几乎没有样本，无法可靠评估。同一个验证集在三个节点上分别测试——① base模型（未经任何训练的Qwen3-14B基座）② SFT冷启动后 ③ DAPO强化学习后。这样可以清晰对比每个阶段的效果提升，也能发现某个阶段是否引入了退化

#### 提示词设计

结合业务梳理了28条评分规则，覆盖四个维度——套联相关、对话质量、医学合规、回复风格与可持续性。人工标注每条数据的扣加分项和最终分数。

#### SFT数据蒸馏

用DeepSeek-R1对标注数据跑推理，蒸馏出思维链。筛选了5K条高质量样本（标准是：推理步骤中命中的规则数量适中(3-6条)、且最终分数和规则与人工标注一致），作为SFT冷启动数据。

> 模型要求遵循 `<reason> -> <compare> -> <score>` 的结构化路径，在打分前完成20+项Rubrics归因判定，把黑盒打分变成有业务理据的逻辑归因。

#### SFT训练细节

基座模型Qwen3-14B-Instruct，LoRA微调（rank=32, alpha=64），训练1个epoch，最终loss降至0.13。

#### Checkpoint选择

没有简单选loss最低的checkpoint，而是综合考察三个维度：
1. **输出格式稳定性**：是否稳定遵循`<reason> -> <compare> -> <score>`的结构
2. **打分偏好准确率**：得分相对大小是否与人工标注一致
3. **规则匹配准确率**：推理中命中的规则是否与标注对齐

> 面试时如果被问"为什么不直接看loss"，可以回答：loss只是训练信号的代理指标，生成式模型的输出质量需要在任务维度上直接评估，特别是格式稳定性和逻辑一致性，这些很难被loss完全反映。


### SFT后的业务指标

- **偏好准确率**：(a,b) 57.1% → 60.9%，(b,a) 41.6% → 45.7%
- **规则匹配度(Pass@5)**：(a,b) 20.4% → 23.5%，(b,a) 11.6% → 15.3%

### 为什么SFT之后还需要RL？

这是面试常问的问题，我从两个维度判断：

1. **Pass@K与Pass@1的差距**：SFT之后对模型做评估，让模型对同一条数据生成K个回复（如K=5），统计Pass@1（第一次就答对）和Pass@K（K次中至少一次答对）的准确率。如果Pass@K显著高于Pass@1，说明模型**有能力给出正确答案**（在多次采样中能命中），但**不够稳定**（不能每次都命中）。这个差距就是RL可以弥补的空间——RL通过奖励信号引导模型更稳定地选择最优推理路径，把"偶尔能做对"变成"经常能做对"

2. **系统性偏差**：SFT后(a,b)和(b,a)的偏好准确率差距仍然较大（60.9% vs 45.7%），说明模型存在明显的位置偏见——同一对候选换个顺序，模型就给出不同的评分。这种系统性偏差SFT很难根治，因为SFT只是在模仿训练数据中的模式，没有显式地优化"位置无关性"这个目标。RL可以通过设计位置一致性奖励函数，针对性地优化这个问题


### 第二阶段：DAPO强化学习


#### RL训练细节

冷启动之后的模型Qwen3-14B-Instruct
 --use_vllm true --vllm_mode colocate --vllm_gpu_memory_utilization 0.1 --tuner_type full --num_train_epochs 1 --per_device_train_batch_size 1 --gradient_accumulation_steps 8 --learning_rate 1e-6 --warmup_ratio 0.05 --weight_decay 0.01 --num_generations 4 --num_iterations 2 --max_completion_length 4096 --temperature 0.9 --top_p 0.9 --top_k 50 --repetition_penalty 1.1 --beta 0.04  --save_steps 100 --save_total_limit 4 --deepspeed zero3 --dataloader_num_workers 4 --dataset_num_proc 4 --bf16 true --truncation_strategy left --log_completions true --log_entropy true --vllm_tensor_parallel_size 8

#### 奖励设计（三重复合奖励，满分6分）

| 奖励函数 | 分值 | 衡量内容 |
|---------|------|---------|
| 得分偏好奖励 | 2分 | 模型给出的分数相对大小是否与标注一致 |
| 规则匹配奖励 | 2分 | 推理中命中的规则的F1值 |
| 位置一致性奖励 | 1分 | 同一对候选交换位置后，得分和规则是否一致（抑制位置偏见） |

#### 为什么用DAPO而不是GRPO？

因为数据集难度差异大，有些数据rollout出的多个回复得到的奖励完全一样（优势为0），GRPO对这类样本梯度为零，相当于浪费了。DAPO的核心改进是：对优势为0的样本多rollout几次，增加得到差异化奖励的概率，让每条数据都能提供有效梯度。

> 面试时可以说"DAPO本质上是在解决GRPO的**信号稀疏问题**"。

#### 训练效果

| 指标 | SFT后 | DAPO后 |
|------|-------|--------|
| 得分偏好奖励 | mean 0.75 | mean 1.5 (↑100%) |
| 规则匹配奖励 | mean 0.4 | mean 0.7 (↑83%) |
| 位置一致性奖励 | mean 0.25 | mean 0.5 (↑133%) |

1.5 -> 2.7

同时三项奖励的**标准差均显著下降**，说明模型输出更加稳定和一致。

### 最终业务指标

- **偏好准确率**：(a,b) 57.1% → 64.3%，(b,a) 41.6% → 50.3%
- **规则匹配度(Pass@5)**：(a,b) 20.4% → 27.2%，(b,a) 11.6% → 19.6%
- **位置一致性**：27.9% → 32.3%

---

