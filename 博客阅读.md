https://thinkingmachines.ai/blog/on-policy-distillation/  

这篇来自Thinking Machines Lab（TML）的博客，提出了一种名为**“On-Policy Distillation”（同策略蒸馏/在策略蒸馏）**的后训练方法。它的核心创新在于：**巧妙地结合了“强化学习”和“传统蒸馏”各自的优点**，让小型语言模型（小模型）能更高效、更强地学习特定领域知识，且成本大幅降低。

### 🎯 背景与问题：大模型虽强，但小模型才是落地的关键

博客首先点明了大型语言模型（LLM）在聚焦领域的专家表现源于“预训练”、“中训练”和“后训练”三个阶段的能力叠加。但在许多实际应用场景中，使用小模型更有优势，例如本地部署保障隐私、持续训练与更新、降低推理成本等。

然而，如何高效地“后训练”出强大的小模型是个挑战。博客将现有方法分为两类，它们各有显著的优缺点：

| 方法               | 数据来源 (On/Off Policy)           | 反馈信号      | 核心问题                                                     |
| :----------------- | :--------------------------------- | :------------ | :----------------------------------------------------------- |
| **强化学习 (RL)**  | 学生模型自己生成的数据 (On-Policy) | 稀疏 (Sparse) | 反馈信息量极少（只告诉你最终对错），不知道具体哪一步对了或错了，学习效率低。 |
| **传统蒸馏 (SFT)** | 教师模型生成的数据 (Off-Policy)    | 密集 (Dense)  | 学生在模仿老师，但没学会处理自己“独特的错误”。一旦犯下新错误，可能再也无法回到正确轨迹上（“复合错误”），导致表现崩溃。 |

### 💡 核心方法：On-Policy Distillation——集两家之长

为了解决以上困境，TML提出了“On-Policy Distillation”。简单来说，它创造了一种“带着教练复盘”的学习模式：

> 由**学生模型**（小模型）亲自上场执行任务，生成解题步骤；然后请一位**教师模型**（大模型）为学生的**每一步**（每一个Token）都提供实时的、密集的评分和指导。

这样一来，学生不仅能知道自己最终“赢了还是输了”，更能清楚地知道是哪一步导致了失败，从而高效地纠正错误。

*   **实现方式**：实现该方法的关键是**最小化学生与教师在每个Token上的概率分布差异（即“反向KL散度”）**。当这个散度为零时，意味着学生的行为与教师完全一致。TML也公开了**伪代码**和基于其**Tinker API**的**完整实现指南**，供开发者复现。

#### 两种主流蒸馏模式对比

为了更清晰地理解“On-Policy Distillation”的位置，可以将其与另外两种主流的蒸馏模式进行对比：

*   **离线蒸馏 (Off-Policy Distillation)**：传统的SFT模式，学生学习教师生成的静态数据集，易出现分布偏移和复合错误。
*   **对抗蒸馏 (Adversarial Distillation)**：引入一个与学生模型协同进化的“判别器”（Reward Model），动态地判断学生输出是否像教师，从而提供训练信号。这在一定程度上缓解了分布偏移问题。
*   **在线策略蒸馏 (On-Policy Distillation)**：本博客的核心，学生直接在自己的输出轨迹上获得教师跨词元级别的监督，从根本上解决了分布偏移问题，并提供最密集的反馈。

### 🚀 实验与结果：成本更低，效果更好

TML分别在“数学推理”和“内部聊天助手”两个任务上验证了On-Policy Distillation的强大性能，并发布了详细的**Tinker Cookbook**供开发者复现实验。具体实验[**复现了Qwen3的技术报告成果**](https://arxiv.org/abs/2505.09388)，使用强大的**Qwen3-32B作为教师**，去训练更小的**Qwen3-8B-Base作为学生**。

**1. 数学推理任务**
*   **对比RL**：训练Qwen3-8B模型解决AIME'24数学题，目标是达到70%的准确率。结果显示，**On-Policy Distillation的成本仅为强化学习方法的十分之一**。
*   **对比SFT**：实验揭示了一个**对数-线性关系**：若仅用SFT达到70%的准确率，需要近200万个样本；而使用On-Policy Distillation，仅需在40万个样本的基础上训练**150步**即可达成，训练效率提升了**9到30倍**。

**2. 内部聊天助手任务**
*   **能力平衡**：在为公司内部文档定制的聊天助手任务中，On-Policy Distillation不仅能提升模型在特定领域的性能，还能**完全恢复或保持模型的通用指令遵循能力（如IF-eval分数不变）**，解决了传统训练中“能力遗忘”的痛点。
*   **多教师整合**：该方法可以高效地整合来自不同领域的多个专家教师模型，实现模型能力的“合并”。

### 💎 总结与影响

On-Policy Distillation的精妙之处在于，它为小模型的后训练提供了一种**“优雅”的混合方案**。它通过结合On-Policy和Dense Reward的优势，让强大的教师模型扮演“过程奖励模型”的角色，为学生提供逐词元、无法被“破解”的密集指导。这使得开发者能够以**极低的成本**，训练出在专业领域**性能强大**，并能**持续学习**更新（如整合最新公司文档）的小模型。

当然，该方法仍有探索空间。例如，如何将这种基于蒸馏的奖励与来自序列级环境的奖励相结合，是未来一个有趣的研究方向。

> 值得一提的是，这篇博客的作者之一Kevin Lu曾是OpenAI的研究者，而Thinking Machines Lab的CEO是OpenAI前CTO Mira Murati。博客显示该工作受到了通义千问Qwen团队研究的启发，且实验中大量使用了Qwen3系列模型。