# VLLM

[(1 封私信 / 24 条消息) 大模型推理框架vLLM原理详解！ - 知乎](https://zhuanlan.zhihu.com/p/1953390686150844488)

vLLM 引入了 **PagedAttention、Prefix Sharing、Continuous Batching** 和 **KV Cache 淘汰策略**，形成高性能推理方案：

- **PagedAttention**：将 KV Cache 分页管理，按需分配逻辑页，解决内部与外部显存碎片问题。
- **Prefix Sharing**：相同前缀请求共享 KV Cache，节省显存并减少重复计算，分叉时通过 Copy-on-Write 保证正确性。
- **Continuous Batching**：动态批处理机制，让新请求随时加入正在执行的 batch，提高 GPU 利用率 并 降低请求延迟。
- **KV Cache 淘汰策略**：显存不足时先换到 CPU 内存，内存不足时再淘汰，必要时重新计算。



# 1. PagedAttention（KV Cache 分页管理）

## 先铺垫：KV Cache 是什么

大模型自回归生成时，每生成一个 token 都要重复计算之前所有 token 的 K、V 向量，所以会把历史 KV 缓存起来，这就是 **KV Cache**，它占推理显存的 70%~90%。

## 传统 KV Cache 的问题

传统方式要求**整块连续显存**存储一个请求的 KV Cache，就像给一本书必须分配一整本空白纸，不能散着放：

- 不同请求序列长度参差不齐，会产生大量**显存碎片**（小空隙用不上）；
- 长序列请求容易因 “找不到连续大块显存” 直接 OOM；
- 显存利用率极低，很多空间被浪费。

## PagedAttention 怎么做（核心）

直接借鉴**操作系统虚拟内存分页**思想：

1. 把整个显存划分为**固定大小的 “页”**（比如 16KB 一页）；
2. **一个请求的 KV Cache 不再需要连续存储，而是拆成多页**，**逻辑上连续、物理上分散**；
3. 用一个**页表**记录每页的位置，**按需申请页**，不用提前分配整块空间；
4. 支持**非连续内存访问**，注意力计算时按页表读取即可。

## 效果

- **彻底解决显存碎片**，显存利用率从 40%~50% 提升到 **95%+**；
- **支持超长序列、超大 batch**，不会因为找不到连续内存 OOM；
- 是 vLLM 高吞吐的核心基础。

------

# 2. Prefix Sharing（相同前缀 KV 共享）

## 场景

大量用户请求会有**完全相同的前缀**，比如：

- 相同的系统提示词（System Prompt）；
- 相同的历史对话轮次；
- 相同的知识库检索前缀。

## 传统方式的浪费

每个请求都会**独立计算、独立存储**这部分相同前缀的 KV Cache，重复计算 + 重复占显存，极其低效。

## Prefix Sharing 核心逻辑

1. 识别请求之间**完全一致的前缀 token 序列**；

2. 只计算**一份**该前缀的 KV Cache，所有请求**共享这部分缓存**；

3. 当前缀出现分叉（比如用户输入不同问题）时，使用 

   **Copy-on-Write**（写时复制）：

   - 不修改原始共享 KV；
   - 分叉部分新建独立的 KV 页，只复制差异部分。

   

## 效果

- 大幅节省 KV Cache 显存占用（相同前缀越长，节省越多）；
- 减少重复的 KV 计算，提升推理速度；
- 多并发相似请求场景下收益极明显。

------

# 3. Continuous Batching（连续 / 动态批处理）

## 先讲传统静态批处理（旧方式）

1. **攒一批请求 → 组成固定 batch → 一起推理**；
2. **必须等 batch 里所有请求都生成完成**，才能释放资源、换新 batch；
3. 问题：batch 里有长有短，短请求早结束了，GPU 还要空等长请求，利用率极低、延迟高。

## Continuous Batching（动态批处理）

vLLM 核心机制，**无等待动态调度**：

1. 不固定 batch，GPU 执行时**实时维护一个动态执行组**；
2. 任何一个请求**生成完毕**，立刻从 batch 中踢出，释放显存资源；
3. 新到达的请求**随时加入**当前执行 batch，不需要攒满、不需要等待；
4. 调度器持续迭代，GPU 几乎永远满载。

## 效果

- GPU 利用率从静态批的 30%~60% 拉到 **90%+**；
- 请求排队时间大幅降低，平均延迟显著下降；
- 相同显卡下，吞吐提升数倍。