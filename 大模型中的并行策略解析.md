# 大模型中的并行策略解析

大模型里的**并行策略**，核心就是解决一个问题：

**模型太大/数据太长/数据太多，单张显卡放不下、算不动，就把任务拆到多张卡上一起算。**

工业界最主流、你必须懂的就 4 个：

**DP、TP、PP、CP**，再加一个进阶的 **ZeRO**。

下面用**最直白、不绕弯**的方式讲清楚：

---

# 1. DP —— Data Parallelism 数据并行

**最简单、最基础、最先用到的并行。**

### 核心一句话

**每张卡都放一整份完整模型，只把数据切分开。**

### 怎么做

- 把一批数据（batch）切成 N 份，N 卡各算 1 份

- 前向：各自算

- 反向：各自算梯度 → **All-Reduce 把所有卡梯度求和平均**

- 更新参数：所有卡保持模型完全一样

### 优点

- 实现最简单

- 卡之间通信少

- 不改变模型结构

### 缺点

- **每张卡都要存完整模型 + 完整优化器状态**

- 模型一大（比如 7B 以上），单卡显存直接爆，DP 就用不了了

---

# 2. TP —— Tensor Parallelism 张量并行（Megatron 核心）

**解决：单层太大，单卡放不下这一层的权重。**

### 核心一句话

**把“一层内部的权重张量”切开，跨多张卡共同算完这一层。**

### 切哪里？

只切 Transformer 里的**大矩阵**：

- **Attention 的 Q/K/V 投影层**

- **FFN 的两个线性层 W1、W2**

例：

一个线性层权重形状 `[d, 4d]`

TP 会把它**按列切**分给两张卡：

- 卡1：`[d, 2d]`

- 卡2：`[d, 2d]`

计算完再通过 All-Gather 合并结果。

### 特点

- **层内并行**，不跨层

- 通信量小，速度快

- 单卡显存压力大幅降低

### 适用

- 模型层的权重太大，单卡塞不下

- LLaMA、Falcon、GPT 系列都大量用 TP

---

# 3. PP —— Pipeline Parallelism 流水线并行

**解决：模型层数太多，整份模型塞不进单卡。**

### 核心一句话

**把模型“按层切开”，不同卡放不同层，像流水线一样接力计算。**

例：

100 层 Transformer → 4 张卡

- 卡 0：层 0~24

- 卡 1：层 25~49

- 卡 2：层 50~74

- 卡 3：层 75~99

数据流过卡0 → 卡1 → 卡2 → 卡3。

### 优点

- 能跑**超大模型**（千亿、万亿参数）

- 每张卡只存一小部分层

### 缺点

- 会有**流水线气泡（bubble）**

前面卡算完了，后面卡还没好，卡会空等

- 推理速度会变慢

---

# 4. 进阶：ZeRO（来自 DeepSpeed）

**不是新并行，而是 DP 的超级优化版。**

DP 最大问题：

每张卡都存：模型权重 + 梯度 + 优化器状态（如 Adam 动量），太费显存。

ZeRO 把这三部分**也切分存到不同卡**：

- ZeRO-1：切**优化器**状态

- ZeRO-2：切**优化器** + **梯度**

- ZeRO-3：切**优化器** + **梯度** + **权重**

**效果：单卡显存占用暴降，可以训超大模型。**

---

# 最关键：它们怎么组合？（工业界标准 3D 并行）

现代大模型训练/推理，**永远是混合并行**：

### 标准组合（Megatron 3D Parallelism）

1. **DP**：切数据

2. **TP**：切单层张量

3. **PP**：切模型层

长文本再加：

1. **CP**：切序列长度

---

# 极简总结（背会就够）

- **DP**：切数据，不切模型

- **TP**：切**一层内部的张量**

- **PP**：切**模型的层**

- **CP**：切**输入序列长度**

- **ZeRO**：优化 DP，省显存





在深度学习的大模型训练中，**DDP (Distributed Data Parallel，分布式数据并行)** 是最基础、也是应用最广泛的并行策略之一。

简单来说，DDP 的核心逻辑是：**模型不动，数据拆分。**

------

### 1. DDP 的核心工作原理

在 DDP 模式下，训练任务会被复制到多个 GPU（通常称为 Rank）上。其具体流程如下：

1. **模型副本 (Replication)：** 每个 GPU 上都加载一个完全相同的模型副本。
2. **数据分片 (Sharding)：** 每一轮（Step）训练时，整批数据（Batch）会被平分到各个 GPU 上。例如，总 Batch Size 是 128，有 4 张显卡，那么每张卡只负责跑 32 个样本。
3. **独立前向与反向传播：** 每张显卡独立计算自己那部分数据的前向传播（Forward）和反向传播（Backward），得到各自的**梯度（Gradients）**。
4. **梯度同步 (All-Reduce)：** 这是最关键的一步。由于各卡算出的梯度不同，模型需要通过通信协议（通常是 NCCL）进行 **All-Reduce** 操作，将所有卡的梯度求平均，确保每张卡拿到的梯度最终是一致的。
5. **更新参数：** 每张卡利用同步后的相同梯度，独立更新自己的模型参数。因为起点相同、梯度相同，更新后的模型在所有卡上依然保持一致。

------

### 2. 为什么 DDP 比早期的 DP (DataParallel) 更强？

在 PyTorch 中，早期有一个 `DataParallel` (DP) 模块，但现在基本已被 DDP 取代。两者的主要区别在于：

| **特性**     | **DP (DataParallel)**                                        | **DDP (Distributed Data Parallel)**                          |
| ------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **架构**     | **单进程、多线程**                                           | **多进程**（每张卡一个独立进程）                             |
| **瓶颈**     | 有“主卡”概念，主卡负责分发数据和汇总梯度，容易成为通信瓶颈。 | **无主从之分**，采用 Ring-AllReduce 等高效算法，通信负载均衡。 |
| **GIL 限制** | 受 Python GIL（全局解释器锁）限制，效率较低。                | 避开了 GIL 限制，扩展性极强。                                |
| **速度**     | 较慢                                                         | **显著更快**                                                 |

------

**Ring-AllReduce 是 DDP (Distributed Data Parallel) 分布式训练中核心的集合通信算法**，由百度在 2017 年提出，后被 NCCL 等主流通信库采用。它通过**环形拓扑结构**实现去中心化的梯度聚合，解决了传统参数服务器架构中主节点通信瓶颈问题，是分布式深度学习高效同步的关键技术。

------

### 3. DDP 在大模型 (LLM) 时代的局限性

虽然 DDP 很高效，但它有一个致命的弱点：**显存冗余**。

在 DDP 中，**每张显卡都必须存储一份完整的模型参数、优化器状态和梯度**。

- 如果一个模型有 70B 参数，单张显卡根本塞不下整个模型，这时 DDP 就跑不动了。
- **解决方案：** 这时就需要进阶方案，比如 **FSDP (Fully Sharded Data Parallel)** 或 **DeepSpeed ZeRO**。它们在 DDP 的基础上，把模型参数和优化器状态也切分到了不同卡上，从而支持训练更大的模型。

------

### 总结

**DDP 是分布式训练的基石。** 如果你的模型单卡能塞得下，DDP 几乎是性能最强、最稳定的选择。但如果你在练百亿、千亿规模的大模型，DDP 通常会作为“底层组件”，与张量并行 (TP) 或流水线并行 (PP) 结合使用。

**你想深入了解 DDP 的具体代码实现（比如 `DistributedSampler` 的用法），还是想对比它与 FSDP/ZeRO 的区别？**



简单来说，**DDP 是“空间换时间”**（效率极高但费显存），而 **FSDP/ZeRO 是“时间换空间”**（显存利用率极高但通信开销略大）。

如果说 DDP 是让每个员工都背着一整套工具箱（模型）干活，那么 FSDP/ZeRO 就是让大家把工具箱拆开，每人只拿一部分，用的时候再互相借。

------

### 1. 核心差异：显存里到底存了什么？

在大模型训练中，显存主要被三样东西占满：**优化器状态 (Optimizer States)**、**梯度 (Gradients)** 和 **参数 (Parameters)**。我们通常统称为 **OSGP**。

#### DDP (数据并行)

- **策略：** 每个 GPU 都存一份完整的 OSGP。
- **缺点：** 冗余极大。如果模型本身就有 40GB，每张卡都要吃掉 40GB，这还没算激活值（Activations）。

#### ZeRO (零冗余优化器) & FSDP

ZeRO 是微软 DeepSpeed 提出的概念，分为三个阶段。FSDP (Fully Sharded Data Parallel) 本质上是 PyTorch 对 ZeRO-3 的官方实现。

1. **ZeRO-1：** 只把 **优化器状态 (O)** 切分到各个 GPU。
2. **ZeRO-2：** 把 **优化器状态 (O) + 梯度 (G)** 都切分了。
3. **ZeRO-3 / FSDP：** 把 **优化器状态 (O) + 梯度 (G) + 参数 (P)** 全部切分。每张卡只存 $1/N$ 的模型（$N$ 为 GPU 数量）。

------

### 2. 深度对比表

| **特性**         | **DDP**             | **ZeRO-1 / ZeRO-2**   | **ZeRO-3 / FSDP**                  |
| ---------------- | ------------------- | --------------------- | ---------------------------------- |
| **显存占用**     | 最高 (全量冗余)     | 中等 (切分状态和梯度) | **最低** (全量切分)                |
| **通信开销**     | 较低 (只需同步梯度) | 与 DDP 相当           | **最高** (前向和后向都要通信参数)  |
| **模型规模限制** | 受限于单卡显存      | 可跑数倍于单卡的模型  | **可跑极大规模模型** (百亿/千亿级) |
| **实现复杂度**   | 简单                | 中等                  | 较高 (涉及复杂的预取和内存管理)    |
| **适用场景**     | 显存富余、追求速度  | 显存较紧、平衡性能    | 显存极度紧张、超大规模模型         |

------

### 3. FSDP/ZeRO-3 是怎么运行的？

既然模型参数被拆散了，训练时是怎么跑起来的呢？

- **前向传播时：** 当计算到某一层（Layer）时，GPU A 会从其他 GPU 那里“借”来（All-Gather）这一层缺失的参数，算完立刻就把借来的参数删掉，只保留自己那份 $1/N$ 的碎片。
- **后向传播时：** 同样的逻辑，算梯度时再借一次参数，算完梯度并完成同步后，依然只留碎片。

**这就是为什么它能省显存：** 在任何给定的时刻，显存里只有当前正在计算的那一层的完整参数，而不是整个模型。

------

### 4. 总结与建议

- **选 DDP 的时机：** 你的模型比较小（比如 BERT、ResNet，或者 7B 以下的模型在 80G 显卡上），单卡显存绰绰有余。DDP 的通信最少，训练速度最快。
- **选 ZeRO-2 的时机：** 这是一个“甜点位”。它能省下大量优化器状态显存，且几乎不增加额外的通信负担（相比 DDP）。大部分中等规模模型的首选。
- **选 FSDP / ZeRO-3 的时机：** 你在挑战 **LLM (大语言模型)**。单卡连模型权重都塞不下，必须靠切分参数才能跑起来。虽然通信变多了，但它是你跑通大规模训练的唯一途径。

------

**你是正在准备搭建一套大模型训练环境，还是在优化现有的训练速度？** 如果需要，我可以帮你看看具体的 `DeepSpeed` 配置文件或者 `Torch FSDP` 的初始化参数设置。



## DP和DDP的区别



在深度学习的工程实践中，**DP (DataParallel)** 和 **DDP (DistributedDataParallel)** 是 PyTorch 提供的两种主要并行策略。虽然它们都属于“数据并行”，但底层的实现逻辑和运行效率有着天壤之别。

以下是它们在架构、通信、性能及显存管理方面的深度对比。

------

### 1. 架构本质：多线程 vs 多进程

这是两者最根本的区别，决定了后续所有的性能表现。

- **DP (单进程多线程)：** 所有的计算逻辑运行在一个进程中。主线程在“主卡”（通常是 `cuda:0`）上控制，然后通过 Python 的多线程机制将任务分发到其他 GPU 线程。
- **DDP (多进程)：** 为每一张显卡创建一个独立的 Python 进程。每个进程拥有自己的优化器（Optimizer）、模型副本和独立的 Python 解释器。

> **核心影响：** 由于 Python 存在 **GIL (全局解释器锁)**，DP 的多线程无法实现真正的并行。在数据量大或模型复杂时，主线程的开销会成为巨大的瓶颈。而 DDP 绕过了 GIL，实现了真正的多机多卡并行。

------

### 2. 通信模式：主从模式 vs 环形/树形 All-Reduce

数据的交换方式直接决定了带宽利用率。

#### DP：中心化通信 (Master-Slave)

1. **分发：** 每轮迭代，主进程将 Input Data 分发给各张卡。
2. **复制：** 主进程将最新的模型参数分发给各张卡。
3. **回收：** 各卡计算完后，将输出（Outputs）或梯度（Gradients）传回主进程。
4. **汇总：** 主进程在主卡上进行损失计算和梯度更新。

#### DDP：去中心化通信 (All-Reduce)

1. **各司其职：** **每个进程独立从磁盘加载自己那部分数据**（通过 `DistributedSampler`）。
2. **梯度同步：** 在反向传播过程中，DDP 利用 **Ring-AllReduce** 或 **Tree-AllReduce** 算法，让**各张卡直接进行梯度交换并求均值**。
3. **独立更新：** **既然所有卡拿到的平均梯度是一致的，那么每个进程独立更新自己的参数，最终结果仍然是同步的**。

------

### 3. 性能表现：为什么 DDP 远快于 DP？

- **减少数据搬运：** DP 在每一轮 Forward 之前都要重新分发模型参数，而 DDP 仅在初始化时分发一次，之后只同步梯度。
- **负载均衡：** * 在 DP 中，**主卡（GPU 0）非常累**。它要负责分发数据、收集结果、计算 Loss、更新梯度。这导致主卡的显存占用远高于其他卡，常常出现“一卡爆显存，他卡在围观”的现象。
  - 在 DDP 中，计算和通信负载被均匀分配到所有卡上，显存占用基本一致。
- **通信与计算重叠：** DDP 采用了 **Bucketing (桶机制)**。在反向传播计算后面层的梯度时，DDP 就可以开始同步已经算好的层，不必等到整个模型算完才通信。

------

### 4. 关键差异对比表

| **特性**       | **DP (DataParallel)**                   | **DDP (DistributedDataParallel)**     |
| -------------- | --------------------------------------- | ------------------------------------- |
| **底层实现**   | Python 多线程                           | **多进程 (C++ / NCCL)**               |
| **通信协议**   | 无专用协议，效率低                      | **NCCL** (NVIDIA 优化) / Gloo         |
| **支持场景**   | 仅单机多卡                              | **单机多卡 & 多机多卡**               |
| **扩展性**     | 差 (卡越多效率越低)                     | **极强** (可扩展至数千张 GPU)         |
| **代码复杂性** | 极简 (`model = nn.DataParallel(model)`) | 略复杂 (需初始化进程组、设置 Sampler) |
| **GPU 负载**   | 不均衡 (主卡压力极大)                   | **完全均衡**                          |

------

### 5. 什么时候该用哪一个？

- **使用 DP 的唯一场景：** 你在进行非常快速的实验原型开发，且你的模型很小、只有 2-4 张卡，懒得去写 DDP 繁琐的初始化代码。
- **使用 DDP 的场景：** **几乎所有正式的训练任务。** 无论是为了更快的训练速度，还是为了训练更大的模型，DDP 都是目前工业界的标准实践。

> **小贴士：** 在使用 DDP 时，由于每个进程独立加载数据，务必使用 `DistributedSampler` 来确保每个进程在每个 Epoch 拿到的是不同的数据子集，否则你的模型就变成了在同一堆数据上反复无效训练。

**既然 DDP 已经解决了梯度同步的效率问题，你是否想了解在大模型显存压力更大的情况下，如何进一步使用 FSDP 来切分模型参数？**